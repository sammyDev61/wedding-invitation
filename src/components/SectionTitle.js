function SectionTitle({ text }) {
  return (
    <h1
      style={{
        marginBottom: "16px",
        fontSize: "24px",
        textAlign: "center",
        color: "#fff",
      }}>
      {text}
    </h1>
  );
}

export default SectionTitle;
